# AGENTS.md
This repository follows the Nizam protocol. Before doing anything:
1. Read `PROTOCOL.md`.
2. Read `START.md` in the private data repo (or the owner's hosted `/start` URL).
3. State your badge from `agents.json`, claim a task in `LOG.md`, work, write back, hand off.
Never write secrets into any file here.
